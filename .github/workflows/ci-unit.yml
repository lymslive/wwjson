name: Unit Tests

on:
  push:
    branches:
      - main
    paths:
      - 'include/wwjson.hpp'
      - '.github/workflows/ci-unit.yml'
  workflow_dispatch:
    inputs:
      cmake_args:
        description: 'CMake configuration arguments. Default: -DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_UNIT_TESTS=ON'
        required: false
        default: '-DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_UNIT_TESTS=ON'
      test_args:
        description: 'Command line arguments for unit tests. Default: --cout=silent'
        required: false
        default: '--cout=silent'

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    
    env:
      CMAKE_ARGS: ${{ github.event.inputs.cmake_args || '-DCMAKE_BUILD_TYPE=RelWithDebInfo -DBUILD_UNIT_TESTS=ON' }}
      TEST_ARGS: ${{ github.event.inputs.test_args || '--cout=silent' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential
        
    - name: Build release version
      run: |
        mkdir -p build-release
        cd build-release
        echo "Running cmake command: cmake $CMAKE_ARGS .."
        cmake $CMAKE_ARGS ..
        make -j4
        
    - name: Run unit tests
      run: |
        # Prepare test command with default arguments
        TEST_CMD="./build-release/utest/utwwjson $TEST_ARGS"
        
        # Echo the actual command for verification
        echo "Running command: $TEST_CMD"
        
        # Execute the test
        $TEST_CMD